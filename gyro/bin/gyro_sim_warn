#!/bin/sh
#
# SCRIPT:
#  gyro_sim_warn
#
# FUNCTION:
#  Check for existence of sim directory
#-------------------------------------------------------------

if [ ! -d "$1" ]
  then
  echo "ERROR: $1 is not a valid simulation directory."
  echo "Create the directory first, or use [gyro -g]."
  exit 1
fi

if [ ! -f "$1/input.gyro" ]
  then
  echo "ERROR: input.gyro not found in $1"
  exit 1
fi

if [ ! -f "$1/restart.dat" ]
  then
  cp $GACODE_ROOT/gyro/tools/input/reg01/restart.dat $1 
  echo "INFO: (gyro_sim_warn) A restart.dat file was created."
fi

exit 0
