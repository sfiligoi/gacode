#---------------------------------------------------------------
# Toplevel makefile for pneo: must be executed from gacode/neo
#---------------------------------------------------------------

include ${GACODE_ROOT}/platform/build/make.inc.${GACODE_PLATFORM}

# NOTE: $EXTRA_LIBS already exported by neo/Makefile

all:
	cd src ; make

clean:
	cd src ; make clean

.IGNORE:
