dnl ######################################################################
dnl
dnl Find FacetsIfc includes
dnl
dnl $Id$
dnl
dnl ######################################################################

unset FCIFC_PATH
for i in `echo $SUPRA_SEARCH_PATH | tr ':' ' '`; do
  FCIFC_PATH="$FCIFC_PATH:$i/facetsifc"
done
for i in `echo $SUPRA_SEARCH_PATH | tr ':' ' '`; do
  FCIFC_PATH="$FCIFC_PATH:$i"
done
FCIFC_PATH="$FCIFC_PATH:$abs_top_srcdir/facetsifc"
TX_LOCATE_PKG(
  [FACETSIFC],
  [$FCIFC_PATH],
  [FacetsIfc.h],
  [-],
  [include, .])
if test "$FOUND_FACETSIFC" = "no"; then
   AC_MSG_ERROR(FacetsIfc library not found. Use --with-facetsifc-dir to set the search directory.)
fi

