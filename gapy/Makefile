#---------------------------
# Toplevel makefile for gapy
#---------------------------

include ${GACODE_ROOT}/platform/build/make.inc.${GACODE_PLATFORM}

all:
	cd src  ; make
	cd EXPRO  ; make

clean:
	cd src ; make clean
	cd EXPRO ; make clean

.IGNORE:
