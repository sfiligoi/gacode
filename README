== GACODE ==

You can make the entire gacode project by typing

$ make

You can build a debug version of all libraries by typing

$ make OPT=debug

The build system is recursive, so this logic works throughout.
