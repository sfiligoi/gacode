#!/bin/sh -x
#
# SCRIPT:
#  exec.GATHETA
#
# FUNCTION:
#  Parallel execution script

simdir=${1}
nmpi=${2}
exec=${3}
nomp=${4}
numa=${5}
mpinuma=${6}
cd $simdir
export OMP_NUM_THREADS=$nomp 
#mpiexec -np $nmpi $exec
mpirun_rsh -hostfile "${MPIHOSTS}" -export-all -np $nmpi $exec
