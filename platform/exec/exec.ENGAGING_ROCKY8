#!/bin/sh
# GACODE Parallel execution script (ENGAGING_ROCKY8)
#
# NOTES:
# Used openmpi-4.1.4

simdir=${1}
nmpi=${2}
exec=${3}
nomp=${4}

echo $simdir

cd $simdir

mpirun -x OMP_NUM_THREADS=$nomp -np $nmpi --oversubscribe $exec

