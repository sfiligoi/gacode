#===========================================
#Environment for HOPPER_CRAY GACODE_PLATFORM
#===========================================
#!/bin/tcsh

# We can just source the bash version for now because it only has module 
# commands, which are shell-invariant

source ${GACODE_ROOT}/shared/env/env.HOPPER_CRAY
