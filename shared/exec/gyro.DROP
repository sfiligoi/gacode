#!/bin/sh
#
# SCRIPT:
#  gyro.DROP
#
# FUNCTION:
#  Parallel execution script

simdir=${1}
nproc=${2}
exec=${3}

cd $simdir

if [ "$TMPDIR" = "" ]
then
   mpiexec -host head -n $nproc $exec
else
   mpiexec -machinefile $TMPDIR/machines -n $nproc $exec
fi
