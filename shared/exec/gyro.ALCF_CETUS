#!/bin/sh
#
# SCRIPT:
#  gyro.ALCF_CETUS
#
# FUNCTION:
#  Parallel execution script

simdir=${1}
nmpi=${2}
exec=${3}
nomp=${4}
numa=${5}
mpinuma=${6}
  
cd $simdir

echo "runjob --block $COBALT_PARTNAME --np $nmpi --ranks-per-node $mpinuma --envs OMP_NUM_THREADS=$nomp --verbose 4 : $exec" > out.exec
runjob --verbose 4 --block $COBALT_PARTNAME --np $nmpi --ranks-per-node $mpinuma --envs OMP_NUM_THREADS=$nomp : $exec
